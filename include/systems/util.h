#pragma once
#include "main.h"

extern double constrainAngle(double angle);
extern double convertToRad (double angle);
extern double convertToDeg (double angle);
extern double encToInch (double encvalue);
extern int sgn (double x);
extern double mmToEnc (double x);
extern double inchToEnc (double inch);